#
# libconfig readable description of a task set with one task
#

#
# tasks -- top level container for all tasks
#
tasks = (
    {
        name = "t.1";
	period = 8;
	deadline = 8;
	threads = 1;
	wcet = (2);
    }
);
